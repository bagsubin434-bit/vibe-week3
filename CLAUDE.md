# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 언어

모든 대화와 커밋 메시지는 한국어로 작성한다.

## 디자인 규칙

버튼은 연보라(#bb8cff)·둥근 모서리 12px 스타일을 따른다.

## Project status

This repository is currently empty — it contains only a `README.md`. There is no build system, no dependencies, no source code, and no tests yet.

When code is added to this project, update this file with:
- Build, lint, and test commands (including how to run a single test)
- High-level architecture notes once the codebase structure is established
